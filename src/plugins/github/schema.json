{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "description": "The GitHub action to perform: listIssues, createIssue, or getRepoDetails"
    },
    "repo": {
      "type": "string",
      "description": "The target repository in owner/repo format (e.g. 'octocat/Hello-World')"
    },
    "title": {
      "type": "string",
      "description": "Optional title for creating an issue"
    },
    "body": {
      "type": "string",
      "description": "Optional body text for creating an issue"
    }
  },
  "required": ["action", "repo"]
}
